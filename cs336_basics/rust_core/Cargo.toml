[package]
name = "rust_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_core"
crate-type = ["cdylib"]  # Required for Python extension modules

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }
