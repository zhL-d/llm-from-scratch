version: "3.9"

services:
  bpe:
    image: cs336-bpe:dev
    build:
      context: .
      tags: ["cs336-bpe:dev"]   # builds only if image missing or you run with --build
    working_dir: /app
    volumes:
      - ./:/app                 # bind-mount code for fast iteration
    env_file:
      - .env       # env file location
    command: ["./start.sh"]
    # healthcheck:
    #   test: ["CMD-SHELL", "test -f /app/cs336_basics/outputs/health.ok || exit 1"]
    #   interval: 30s
    #   timeout: 5s
    #   retries: 5
    #   start_period: 30s         # optional: give the job a moment to start


    
